# Policy packs

Licence policy packs live at `/policies/licences/*.yml` (repo root), loaded
at runtime by `cleanroom.licence.policy`. Not duplicated here -- see
`references/licence-discovery.md` for how to read them.
