# Schemas

This skill validates against the project's canonical schemas at
`/schemas/*.schema.json` (repo root), loaded at runtime by
`cleanroom.schema_registry`. They are not duplicated here so there is one
source of truth -- see `/schemas/` and `docs/architecture.md`.
